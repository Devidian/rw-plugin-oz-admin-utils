## Public renderer map

- Enabled terrain map exports can now be read directly by standalone renderers.
- Disabled map exports remain unavailable; player, configuration, and other native exports remain protected.
- The package ships JSON settings and translations while preserving existing-server migration and backups.
