# OZ Admin Utils 0.11.0

- Experimental RenderWorld can render a changed outdoor chunk after an opted-in player enters it.
- Server, administrator, and player opt-ins keep the feature disabled by default, with a bounded selectable resolution.
- RenderWorld settings and player controls are fully localized in German and English.
