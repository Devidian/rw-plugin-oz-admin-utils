# Live player position source

The cross-repository objective, contract, migration, rollback, risks, and
validation checklist are maintained in `../../docs/active/live-player-position-sync.md`.

This repository owns bounded sampling of currently connected player positions
into its existing SQLite database while player-data exposure is enabled.
