# Admin Utils Development test: next-250926b

Objective: blueprint play-time gate, unified Discord channel settings, protected-NPC theft escalation.

Owner: Admin Utils. Uses existing PluginSettings, area permission and prison services. No new runtime dependency beyond the current OZ Tools version.

- [x] Implement configuration, listener dispatch, persistence, and DE/EN text.
- [x] Validate local tests and PluginAPI signatures.
- [x] Validate Admin Utils plugin reload on Development and confirm event registration and state table.
- [x] Manually accept the Development feature set (user confirmation).

Development follow-up: the blueprint restriction now applies to administrators as well. The active Dev setting is `general.blueprintMinPlayHours=9999`; the updated JAR hash matches the scoped build. Admin Utils enabled and the blueprint event was registered at 13:25 UTC.

Risks: existing JSON files retain old logging switches but the runtime ignores them. Channel ID 0 now disables the associated report, including local event logging. No prison configured leaves the prohibited action cancelled and the sixth attempt pending. Rollback: restore the prior Admin Utils JAR and i18n/default settings from the Dev plugin backup; the additional SQLite table is inert under the prior code.
